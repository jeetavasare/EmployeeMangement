@model int
@{
	ViewBag.Title = "404";
}
<div class="alert alert-danger mt-1 mb-1">
	<h4>404 Not found</h4>
	<hr />
	<h5>
		Employee with id @Model cannot be found
	</h5>
</div>
<a asp-action="Index"  asp-controller="home" class="btn btn-outline-success" style="width:auto">Click to see the list of all employees</a>